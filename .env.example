# Copy this file to .env and fill in your values
PORT=3001

# Optional: pre-configure SMTP via environment (overrides UI settings)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=you@gmail.com
# SMTP_PASS=your-app-password
